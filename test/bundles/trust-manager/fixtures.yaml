apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: e2e
spec:
  sources:
    - useDefaultCAs: true
  target:
    configMap:
      key: "ca-certificates.crt"
    secret:
      key: "ca-certificates.crt"
